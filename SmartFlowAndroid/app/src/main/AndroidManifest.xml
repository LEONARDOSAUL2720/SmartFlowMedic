<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permiso para acceder a Internet (requerido para autenticación y llamadas a backend) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/icon_smart_flow"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon_smart_flow"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartFlow"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- Pantalla de inicio para Pacientes -->
        <activity
            android:name=".PacienteHomeActivity"
            android:exported="false" />
        
        <!-- Pantalla de inicio para Médicos -->
        <activity
            android:name=".MedicoHomeActivity"
            android:exported="false" />
    </application>

</manifest>